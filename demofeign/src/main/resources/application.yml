eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
  instance:
    prefer-ip-address: true
    instance-id: 127.0.0.1:8005
server:
  port: 8005
spring:
  application:
    name: feign
feign:
  hystrix:
    enabled: true